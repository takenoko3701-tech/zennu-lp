<style>
  /* ─── 訴求バリエーション専用スタイル(TRUST BADGES / PAIN POINTS / AUTHORITY) ─── */

  .trust-badges {
    background: var(--white);
    border-bottom: 1px solid var(--border);
    padding: clamp(28px, 4vw, 44px) var(--pad);
  }
  .trust-grid {
    max-width: var(--max); margin: 0 auto;
    display: grid; grid-template-columns: repeat(4, 1fr);
    gap: 24px; text-align: center;
  }
  .trust-item { display: flex; flex-direction: column; align-items: center; gap: 6px; }
  .trust-num {
    font-family: var(--display); font-weight: 900;
    font-size: clamp(28px, 3.4vw, 40px); color: var(--orange); line-height: 1;
  }
  .trust-num span:last-child { font-size: 55%; margin-left: 2px; }
  .trust-lbl { font-size: 12px; color: var(--muted); letter-spacing: .06em; }
  .trust-item.badge .trust-badge-mark {
    display: inline-flex; align-items: center; justify-content: center;
    width: 56px; height: 56px; border-radius: 50%;
    background: var(--orange-pale); color: var(--orange);
    font-family: var(--display); font-weight: 800; font-size: 14px; letter-spacing: .04em;
    margin-bottom: 2px;
  }

  .pain-grid {
    display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px 32px;
    max-width: 760px; margin: 0 auto 32px;
  }
  .pain-item {
    display: flex; align-items: center; gap: 12px;
    font-size: 15px; font-weight: 700; color: var(--text); letter-spacing: .02em;
    padding: 14px 18px; background: var(--white); border-radius: 10px;
    border: 1px solid var(--border);
  }
  .pain-mark {
    flex-shrink: 0; width: 26px; height: 26px; border-radius: 50%;
    background: var(--orange); color: #fff; font-weight: 900; font-size: 13px;
    display: flex; align-items: center; justify-content: center;
  }
  .pain-bridge {
    text-align: center; font-size: 15px; font-weight: 700; color: var(--orange);
    letter-spacing: .03em; max-width: 560px; margin: 0 auto;
  }

  .authority-block {
    max-width: 720px; margin: 0 auto; text-align: center;
  }
  .authority-eyebrow {
    font-family: var(--display); font-weight: 700; font-size: 12px;
    color: var(--orange); letter-spacing: .2em; margin-bottom: 16px;
  }
  .authority-title {
    font-family: var(--serif); font-weight: 700;
    font-size: clamp(20px, 2.6vw, 30px); line-height: 1.6; letter-spacing: .02em;
    color: var(--text); margin-bottom: 22px;
  }
  .authority-body {
    font-size: 14px; color: var(--muted); line-height: 2; letter-spacing: .03em;
  }

  @media (max-width: 860px) {
    .trust-grid { grid-template-columns: repeat(2, 1fr); gap: 24px 16px; }
    .pain-grid { grid-template-columns: 1fr; }
  }
</style>
