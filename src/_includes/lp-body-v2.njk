<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ site.meta.title }}</title>
  <meta name="description" content="{{ site.meta.description }}">
  <link rel="canonical" href="{{ site.meta.url }}/2/">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
  <link rel="shortcut icon" href="/favicon.ico">
  <meta name="robots" content="noindex, follow">

  <!-- Open Graph / SNSシェア -->
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="ZenNu WELLNESS DESIGN">
  <meta property="og:title" content="{{ site.meta.title }}">
  <meta property="og:description" content="{{ site.meta.description }}">
  <meta property="og:url" content="{{ site.meta.url }}/2/">
  <meta property="og:image" content="{{ site.meta.url }}{{ site.meta.ogImage }}">
  <meta property="og:locale" content="ja_JP">
  <meta name="twitter:card" content="summary_large_image">

  <!-- 構造化データ（ローカルビジネス / ジム） -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "HealthClub",
    "name": "ZenNu WELLNESS DESIGN",
    "description": "{{ site.meta.description }}",
    "url": "{{ site.meta.url }}/",
    "image": "{{ site.meta.url }}{{ site.meta.ogImage }}",
    "telephone": "{{ site.contact.telDisplay }}",
    "priceRange": "¥¥",
    "address": {
      "@type": "PostalAddress",
      "postalCode": "860-0845",
      "addressRegion": "熊本県",
      "addressLocality": "熊本市中央区",
      "streetAddress": "上通町6-15 オークス第二ビル 2F",
      "addressCountry": "JP"
    },
    "geo": {
      "@type": "GeoCoordinates",
      "latitude": "{{ site.meta.lat }}",
      "longitude": "{{ site.meta.lng }}"
    },
    "openingHoursSpecification": [{
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"],
      "opens": "07:00",
      "closes": "22:00"
    }]
  }
  </script>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500;600;700;800;900&family=Noto+Sans+JP:wght@300;400;500;700;900&display=swap" rel="stylesheet">
  {% include "site-style.njk" %}
  <style>
    /* /2/ only: diet-first appeal variant */
    .site-header:not(.scrolled) .logo-img { filter: brightness(0) invert(1); }
    .site-header:not(.scrolled) .site-nav a { color: rgba(255,255,255,.78); }
    .site-header:not(.scrolled) .site-nav a:hover { color: #fff; background: rgba(255,255,255,.08); }
    .site-header:not(.scrolled) .ham-btn span { background: #fff; }
    .v2-hero {
      position: relative;
      min-height: 760px;
      padding: 64px 0 56px;
      overflow: hidden;
      background: #1d1916;
      color: #fff;
    }
    .v2-hero::after {
      content: '';
      position: absolute;
      inset: auto -12% -38% auto;
      width: 54vw;
      height: 54vw;
      border: 1px solid rgba(232,112,48,.25);
      border-radius: 50%;
      box-shadow: 0 0 0 70px rgba(232,112,48,.04), 0 0 0 140px rgba(232,112,48,.025);
      pointer-events: none;
    }
    .v2-hero-photo {
      position: absolute;
      inset: 0 0 0 45%;
    }
    .v2-hero-photo::before {
      content: '';
      position: absolute;
      inset: 0;
      z-index: 1;
      background:
        linear-gradient(90deg, #1d1916 0%, rgba(29,25,22,.9) 14%, rgba(29,25,22,.3) 55%, rgba(29,25,22,.38) 100%),
        linear-gradient(0deg, rgba(29,25,22,.75) 0%, transparent 42%);
    }
    .v2-hero-photo img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      filter: saturate(.75) contrast(1.05);
    }
    .v2-hero-inner {
      position: relative;
      z-index: 2;
      width: min(1200px, 100%);
      margin: 0 auto;
      padding: clamp(52px, 7vw, 92px) var(--pad) 0;
    }
    .v2-hero-kicker {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 24px;
      color: #f5a578;
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .18em;
    }
    .v2-hero-kicker::before { content: ''; width: 34px; height: 1px; background: currentColor; }
    .v2-hero-title {
      max-width: 820px;
      margin-bottom: 22px;
      font-size: clamp(42px, 5.8vw, 78px);
      font-weight: 900;
      line-height: 1.22;
      letter-spacing: -.035em;
    }
    .v2-hero-title > span { display: block; }
    .v2-hero-title em { color: var(--orange); font-style: normal; }
    .v2-hero-lead {
      max-width: 620px;
      margin-bottom: 28px;
      color: rgba(255,255,255,.78);
      font-size: clamp(14px, 1.5vw, 17px);
      font-weight: 500;
      line-height: 2;
      letter-spacing: .04em;
    }
    .v2-hero-actions { display: flex; align-items: center; flex-wrap: wrap; gap: 14px 20px; }
    .v2-primary-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 290px;
      padding: 18px 26px;
      background: var(--orange);
      color: #fff;
      font-size: 15px;
      font-weight: 800;
      letter-spacing: .07em;
      box-shadow: 0 12px 32px rgba(232,112,48,.3);
      transition: transform .2s, background .2s;
    }
    .v2-primary-btn:hover { transform: translateY(-2px); background: #d4612a; }
    .v2-hero-note { color: rgba(255,255,255,.58); font-size: 11.5px; line-height: 1.7; }
    .v2-proof-wrap { width: min(760px, 100%); margin-top: 46px; }
    .v2-proof-heading {
      display: grid;
      grid-template-columns: 1fr auto 1fr;
      align-items: center;
      gap: 14px;
      margin-bottom: 15px;
      color: rgba(255,255,255,.72);
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .14em;
      text-align: center;
    }
    .v2-proof-heading::before,
    .v2-proof-heading::after { content: ''; height: 1px; background: linear-gradient(90deg, transparent, rgba(245,165,120,.65)); }
    .v2-proof-heading::after { background: linear-gradient(90deg, rgba(245,165,120,.65), transparent); }
    .v2-proof-row {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 12px;
    }
    .v2-proof {
      position: relative;
      min-height: 166px;
      overflow: hidden;
      padding: 20px 20px 17px;
      border: 1px solid rgba(245,165,120,.38);
      border-radius: 14px;
      background: linear-gradient(145deg, rgba(255,255,255,.97), rgba(251,239,229,.92));
      color: #241e1a;
      box-shadow: 0 14px 34px rgba(0,0,0,.22);
      isolation: isolate;
    }
    .v2-proof::before {
      content: '';
      position: absolute;
      z-index: -1;
      width: 118px;
      height: 118px;
      right: -38px;
      top: -48px;
      border: 18px solid rgba(232,112,48,.08);
      border-radius: 50%;
    }
    .v2-proof::after {
      content: '';
      position: absolute;
      left: 20px;
      right: 20px;
      bottom: 0;
      height: 3px;
      background: linear-gradient(90deg, transparent, var(--orange), transparent);
    }
    .v2-proof-top { display: flex; align-items: center; justify-content: space-between; margin-bottom: 13px; }
    .v2-proof-seal {
      width: 37px;
      height: 37px;
      border: 1px solid rgba(232,112,48,.55);
      border-radius: 50%;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: #fff;
      color: var(--orange);
      font-size: 11px;
      font-weight: 900;
      letter-spacing: .02em;
      box-shadow: 0 0 0 4px rgba(232,112,48,.08);
    }
    .v2-proof-stars { color: #d98a3b; font-size: 9px; letter-spacing: .08em; white-space: nowrap; }
    .v2-proof-label { color: #665b53; font-size: 11px; font-weight: 800; letter-spacing: .06em; }
    .v2-proof-value {
      display: flex;
      align-items: baseline;
      min-height: 49px;
      margin: 1px 0 3px;
      font-size: clamp(30px, 3.5vw, 44px);
      font-weight: 900;
      line-height: 1.05;
      letter-spacing: -.035em;
    }
    .v2-proof-value span { color: var(--orange); font-size: .45em; margin-left: 4px; letter-spacing: .02em; }
    .v2-proof-value.is-text { font-size: clamp(27px, 3.1vw, 39px); letter-spacing: -.02em; }
    .v2-proof-value.is-review::before { content: '★'; margin-right: 5px; color: #d98a3b; font-size: .48em; }
    .v2-proof-desc { color: #7c7068; font-size: 10px; font-weight: 600; line-height: 1.55; }
    .v2-slot {
      position: absolute;
      right: var(--pad);
      bottom: 34px;
      z-index: 3;
      width: 150px;
      height: 150px;
      border-radius: 50%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      background: var(--orange);
      color: #fff;
      box-shadow: 0 12px 36px rgba(0,0,0,.28);
      transform: rotate(5deg);
    }
    .v2-slot small { font-size: 10px; font-weight: 700; letter-spacing: .1em; }
    .v2-slot strong { font-size: 38px; font-weight: 900; line-height: 1.1; }
    .v2-slot span { font-size: 12px; font-weight: 700; }

    .v2-band { background: #f1e4d8; color: #3d3028; }
    .v2-band p { display: flex; justify-content: center; flex-wrap: wrap; gap: 10px 28px; }
    .v2-band span::before { content: '✓'; margin-right: 8px; color: var(--orange); font-weight: 900; }

    .v2-section-head { max-width: 820px; margin-bottom: 42px; }
    .v2-eyebrow {
      margin-bottom: 12px;
      color: var(--orange);
      font-size: 11px;
      font-weight: 800;
      letter-spacing: .2em;
    }
    .v2-title {
      font-size: clamp(26px, 4.3vw, 50px);
      font-weight: 900;
      line-height: 1.45;
      letter-spacing: -.02em;
    }
    .v2-title em { color: var(--orange); font-style: normal; }
    .v2-lead { margin-top: 18px; color: var(--muted); font-size: 14px; line-height: 2.05; }

    .v2-pain-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; }
    .v2-pain-card {
      display: grid;
      grid-template-columns: 44px 1fr;
      gap: 14px;
      align-items: center;
      padding: 22px;
      border: 1px solid var(--border);
      background: #fff;
    }
    .v2-pain-no { color: #d5c6b9; font-size: 13px; font-weight: 800; letter-spacing: .08em; }
    .v2-pain-text { font-size: 14px; font-weight: 700; line-height: 1.7; }
    .v2-pain-answer {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 24px;
      align-items: center;
      margin-top: 28px;
      padding: 28px 32px;
      background: #1d1916;
      color: #fff;
    }
    .v2-pain-answer b { color: var(--orange); font-size: 12px; letter-spacing: .16em; }
    .v2-pain-answer p { font-size: clamp(16px, 2.2vw, 22px); font-weight: 800; line-height: 1.6; }

    .v2-mini-cta {
      padding: 24px var(--pad);
      background: var(--orange-pale);
      border-top: 1px solid #f0d7c7;
      border-bottom: 1px solid #f0d7c7;
    }
    .v2-mini-cta-inner {
      width: min(920px, 100%);
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 22px;
    }
    .v2-mini-cta-copy strong { display: block; font-size: 16px; line-height: 1.5; }
    .v2-mini-cta-copy span { color: var(--muted); font-size: 11.5px; }
    .v2-mini-cta-link {
      flex-shrink: 0;
      padding: 13px 22px;
      border: 1px solid var(--orange);
      background: #fff;
      color: var(--orange);
      font-size: 13px;
      font-weight: 800;
      letter-spacing: .05em;
      transition: color .2s, background .2s;
    }
    .v2-mini-cta-link:hover { background: var(--orange); color: #fff; }

    .v2-method-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; }
    .v2-method-card {
      position: relative;
      overflow: hidden;
      min-height: 390px;
      display: flex;
      align-items: flex-end;
      background: #231e1a;
      color: #fff;
    }
    .v2-method-card img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; opacity: .58; }
    .v2-method-card::after { content: ''; position: absolute; inset: 34% 0 0; background: linear-gradient(transparent, rgba(20,17,15,.96)); }
    .v2-method-content { position: relative; z-index: 1; padding: 26px; }
    .v2-method-no { display: block; color: #f5a578; font-size: 11px; font-weight: 800; letter-spacing: .17em; }
    .v2-method-card h3 { margin: 8px 0 10px; font-size: 20px; line-height: 1.55; }
    .v2-method-card p { color: rgba(255,255,255,.7); font-size: 12.5px; line-height: 1.9; }
    .v2-method-effect {
      display: inline-flex;
      margin-top: 16px;
      padding: 6px 10px;
      border: 1px solid rgba(245,165,120,.5);
      color: #ffc19f;
      font-size: 10px;
      font-weight: 800;
      letter-spacing: .08em;
    }

    .v2-routine { background: #201b18; color: #fff; }
    .v2-routine .v2-title { color: #fff; }
    .v2-routine .v2-lead { color: rgba(255,255,255,.6); }
    .v2-routine-grid { display: grid; grid-template-columns: 1fr 70px 1fr; align-items: stretch; }
    .v2-routine-card { padding: clamp(26px, 4vw, 42px); border: 1px solid rgba(255,255,255,.13); }
    .v2-routine-card.after { border-color: rgba(232,112,48,.75); background: rgba(232,112,48,.08); }
    .v2-routine-label { margin-bottom: 24px; color: rgba(255,255,255,.42); font-size: 11px; font-weight: 800; letter-spacing: .18em; }
    .v2-routine-card.after .v2-routine-label { color: #f5a578; }
    .v2-routine-list { display: flex; flex-direction: column; gap: 16px; }
    .v2-routine-list li { display: flex; gap: 12px; color: rgba(255,255,255,.72); font-size: 13px; line-height: 1.75; }
    .v2-routine-list li span {
      flex: 0 0 25px;
      height: 25px;
      border-radius: 50%;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border: 1px solid rgba(255,255,255,.25);
      color: rgba(255,255,255,.55);
      font-size: 10px;
      font-weight: 800;
    }
    .v2-routine-card.after li span { border-color: var(--orange); background: var(--orange); color: #fff; }
    .v2-routine-arrow { display: flex; align-items: center; justify-content: center; color: var(--orange); font-size: 28px; }

    .v2-proof-intro {
      display: flex;
      align-items: flex-end;
      justify-content: space-between;
      gap: 24px;
      margin-bottom: 36px;
    }
    .v2-proof-note { max-width: 360px; color: var(--light); font-size: 10.5px; line-height: 1.7; text-align: right; }
    .v2-section-footnote { margin-top: 18px; color: var(--light); font-size: 10.5px; line-height: 1.7; }

    .v2-value {
      display: grid;
      grid-template-columns: minmax(0, .92fr) minmax(0, 1.08fr);
      align-items: stretch;
    }
    .v2-value-copy {
      position: relative;
      overflow: hidden;
      padding: clamp(34px, 5vw, 58px);
      background: #201b18;
      color: #fff;
    }
    .v2-value-copy::after {
      content: '';
      position: absolute;
      right: -90px;
      bottom: -130px;
      width: 280px;
      height: 280px;
      border: 1px solid rgba(232,112,48,.32);
      border-radius: 50%;
      box-shadow: 0 0 0 42px rgba(232,112,48,.05);
    }
    .v2-value-copy > * { position: relative; z-index: 1; }
    .v2-value-copy .v2-title { color: #fff; }
    .v2-value-copy .v2-lead { color: rgba(255,255,255,.68); }
    .v2-value-price {
      display: grid;
      grid-template-columns: auto 1fr;
      align-items: end;
      gap: 4px 12px;
      margin-top: 32px;
      padding-top: 24px;
      border-top: 1px solid rgba(255,255,255,.18);
    }
    .v2-value-price small { color: #f5a578; font-size: 11px; font-weight: 800; letter-spacing: .14em; }
    .v2-value-price strong { grid-column: 1 / -1; color: #fff; font-size: clamp(42px, 5vw, 64px); line-height: 1; letter-spacing: -.045em; }
    .v2-value-price strong sup { color: var(--orange); font-size: .48em; vertical-align: .7em; }
    .v2-value-price span { grid-column: 1 / -1; color: rgba(255,255,255,.58); font-size: 11px; font-weight: 700; letter-spacing: .05em; }
    .v2-value-visual {
      position: relative;
      min-height: 520px;
      overflow: hidden;
      background: #171412;
    }
    .v2-value-visual::after {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(0deg, rgba(20,16,14,.9) 0%, rgba(20,16,14,.06) 64%);
    }
    .v2-value-visual img { width: 100%; height: 100%; object-fit: cover; object-position: center; filter: saturate(.82) contrast(1.05); }
    .v2-value-facts {
      position: absolute;
      z-index: 1;
      right: clamp(20px, 3vw, 34px);
      bottom: clamp(20px, 3vw, 34px);
      left: clamp(20px, 3vw, 34px);
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      border: 1px solid rgba(255,255,255,.22);
      background: rgba(24,20,17,.8);
      backdrop-filter: blur(10px);
    }
    .v2-value-fact { padding: 18px 14px; color: rgba(255,255,255,.68); font-size: 10px; font-weight: 700; line-height: 1.6; text-align: center; }
    .v2-value-fact + .v2-value-fact { border-left: 1px solid rgba(255,255,255,.16); }
    .v2-value-fact strong { display: block; margin-bottom: 4px; color: #fff; font-size: 15px; }

    .v2-nutrition-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(400px, .92fr);
      align-items: stretch;
      background: #f7f3ee;
    }
    .v2-nutrition-copy { padding: clamp(38px, 6vw, 72px); }
    .v2-nutrition-points { display: grid; gap: 18px; margin-top: 30px; }
    .v2-nutrition-point { display: grid; grid-template-columns: 30px 1fr; gap: 12px; align-items: start; }
    .v2-nutrition-check {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 30px;
      height: 30px;
      border-radius: 50%;
      background: var(--orange);
      color: #fff;
      font-size: 14px;
      font-weight: 900;
    }
    .v2-nutrition-point strong { display: block; margin-bottom: 5px; font-size: 14px; line-height: 1.6; }
    .v2-nutrition-point p { color: var(--muted); font-size: 11.5px; line-height: 1.75; }
    .v2-nutrition-visual { position: relative; min-height: 590px; overflow: hidden; background: #f5f0e9; }
    .v2-nutrition-visual img { width: 100%; height: 100%; object-fit: cover; object-position: 66% center; }
    .v2-credential-card {
      position: absolute;
      right: clamp(18px, 3vw, 34px);
      bottom: clamp(18px, 3vw, 34px);
      left: clamp(18px, 3vw, 34px);
      display: grid;
      grid-template-columns: 58px 1fr;
      gap: 16px;
      align-items: center;
      padding: 20px;
      border: 1px solid rgba(190,140,66,.5);
      background: rgba(255,255,255,.94);
      box-shadow: 0 18px 45px rgba(52,39,27,.18);
      backdrop-filter: blur(8px);
    }
    .v2-credential-seal {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 58px;
      height: 58px;
      border: 1px solid #b98d4b;
      border-radius: 50%;
      box-shadow: inset 0 0 0 4px #fff, inset 0 0 0 5px rgba(185,141,75,.45);
      color: #a87934;
      font-family: var(--display);
      font-size: 11px;
      font-weight: 900;
      letter-spacing: .08em;
    }
    .v2-credential-card small { display: block; margin-bottom: 4px; color: #a87934; font-size: 9px; font-weight: 800; letter-spacing: .18em; }
    .v2-credential-card strong { display: block; font-size: clamp(16px, 2vw, 22px); line-height: 1.45; }
    .v2-credential-card span { color: var(--muted); font-size: 10px; line-height: 1.6; }
    }
    .v2-value-availability {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 10px 24px;
      margin-top: 20px;
      color: var(--muted);
      font-size: 10.5px;
      font-weight: 700;
      letter-spacing: .04em;
    }
    .v2-value-availability span::before { content: '✓'; margin-right: 7px; color: var(--orange); }

    @media (max-width: 960px) {
      .v2-hero { min-height: auto; padding-bottom: 48px; }
      .v2-hero-photo { position: absolute; inset: 0; height: auto; min-height: 0; margin-top: 0; opacity: .34; }
      .v2-hero-photo::before {
        background:
          linear-gradient(90deg, rgba(29,25,22,.96), rgba(29,25,22,.72)),
          linear-gradient(0deg, #1d1916 0%, transparent 42%);
      }
      .v2-hero-inner { padding-top: 48px; }
      .v2-slot { right: 20px; bottom: 24px; width: 126px; height: 126px; }
      .v2-slot strong { font-size: 32px; }
      .v2-method-grid { grid-template-columns: 1fr; }
      .v2-method-card { min-height: 310px; }
      .v2-routine-grid { grid-template-columns: 1fr; }
      .v2-routine-arrow { min-height: 58px; transform: rotate(90deg); }
      .v2-value { grid-template-columns: 1fr; }
      .v2-value-visual { min-height: 440px; }
      .v2-nutrition-grid { grid-template-columns: 1fr; }
      .v2-nutrition-visual { min-height: 540px; }
    }
    @media (max-width: 640px) {
      .v2-slot { display: none; }
      .v2-hero-inner { padding-top: 40px; }
      .v2-hero-kicker { gap: 8px; margin-bottom: 20px; font-size: 9.5px; letter-spacing: .12em; }
      .v2-hero-kicker::before { width: 24px; }
      .v2-hero-title { max-width: none; font-size: clamp(34px, 9.5vw, 40px); line-height: 1.28; letter-spacing: -.045em; }
      .v2-hero-lead br { display: none; }
      .v2-hero-lead { line-height: 1.9; }
      .v2-primary-btn { min-width: 100%; }
      .v2-proof-wrap { margin-top: 36px; }
      .v2-proof-heading { font-size: 10px; letter-spacing: .08em; }
      .v2-proof-row {
        grid-template-columns: repeat(3, minmax(212px, 1fr));
        gap: 10px;
        overflow-x: auto;
        padding: 0 1px 8px;
        scroll-snap-type: x mandatory;
        scrollbar-width: none;
      }
      .v2-proof-row::-webkit-scrollbar { display: none; }
      .v2-proof { min-height: 138px; padding: 17px 18px 15px; scroll-snap-align: start; }
      .v2-proof-top { margin-bottom: 8px; }
      .v2-proof-value, .v2-proof-value.is-text { min-height: 39px; font-size: 32px; }
      .v2-band p { align-items: flex-start; flex-direction: column; text-align: left; }
      .sec-content { padding-top: 38px; padding-bottom: 60px; }
      .sec-big-label { padding-top: 52px; font-size: clamp(40px, 13vw, 58px); }
      .sec-big-sub { padding-top: 10px; line-height: 1.7; }
      .v2-section-head { margin-bottom: 30px; }
      .v2-title { font-size: clamp(25px, 7.2vw, 31px); line-height: 1.52; }
      .v2-title br, .v2-pain-answer br, .v2-method-card h3 br { display: none; }
      .v2-lead { margin-top: 14px; line-height: 1.9; }
      .v2-pain-grid { grid-template-columns: 1fr; }
      .v2-pain-card { grid-template-columns: 34px 1fr; padding: 18px; }
      .v2-pain-answer { grid-template-columns: 1fr; gap: 8px; padding: 24px; }
      .v2-mini-cta-inner { align-items: stretch; flex-direction: column; }
      .v2-mini-cta-link { text-align: center; }
      .v2-method-card { min-height: 286px; }
      .v2-method-content { padding: 22px; }
      .v2-routine-card { padding: 24px 20px; }
      .v2-routine-label { margin-bottom: 18px; letter-spacing: .1em; }
      .v2-routine-list { gap: 13px; }
      .v2-proof-intro { align-items: flex-start; flex-direction: column; }
      .v2-proof-note { text-align: left; }
      .v2-value-copy { padding: 30px 24px; }
      .v2-value-visual { min-height: 390px; }
      .v2-value-facts { grid-template-columns: 1fr; }
      .v2-value-fact { padding: 10px 12px; text-align: left; }
      .v2-value-fact + .v2-value-fact { border-top: 1px solid rgba(255,255,255,.16); border-left: 0; }
      .v2-value-fact strong { display: inline; margin-right: 8px; }
      .v2-value-availability { align-items: flex-start; flex-direction: column; }
      .v2-nutrition-copy { padding: 38px 24px; }
      .v2-nutrition-visual { min-height: 460px; }
      .v2-nutrition-visual img { object-position: 62% center; }
      .v2-credential-card { grid-template-columns: 48px 1fr; padding: 16px; }
      .v2-credential-seal { width: 48px; height: 48px; font-size: 9px; }
    }
  </style>
  <!-- Google Tag Manager -->
  <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src='https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);})(window,document,'script','dataLayer','GTM-PCDJZ9X9');</script>
  <!-- End Google Tag Manager -->
</head>
<body>
  <!-- Google Tag Manager (noscript) -->
  <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-PCDJZ9X9" height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
  <!-- End Google Tag Manager (noscript) -->

{% include "nav.njk" %}

<!-- ─── HERO: diet-first message ─── -->
<section class="v2-hero" id="top">
  <div class="v2-hero-inner">
    <div class="v2-hero-kicker">KUMAMOTO / DIET &amp; BODY MAKE</div>
    <h1 class="v2-hero-title"><span>「また戻った」を、</span><em>今回で終わりに。</em></h1>
    <p class="v2-hero-lead">筋トレで消費を増やし、3食を整えて摂取とのバランスをつくる。<br>プロがフォーム・運動量・食事を調整するから、極端な我慢に頼らず、体脂肪を落とす行動を続けられます。</p>
    <div class="v2-hero-actions">
      <a href="#reserve" class="v2-primary-btn">無料体験で、痩せ方を相談する →</a>
      <p class="v2-hero-note">初回限定・完全無料<br>体験後の入会は任意です</p>
    </div>
    <div class="v2-proof-wrap">
      <div class="v2-proof-heading">続けやすさと安心で選ばれています</div>
      <div class="v2-proof-row">
        <div class="v2-proof">
          <div class="v2-proof-top"><span class="v2-proof-seal">12M</span><span class="v2-proof-stars">ZENNU</span></div>
          <div class="v2-proof-label">1年継続率</div>
          <div class="v2-proof-value">96<span>%</span></div>
          <div class="v2-proof-desc">長く続けやすいハイブリッド設計</div>
        </div>
        <div class="v2-proof">
          <div class="v2-proof-top"><span class="v2-proof-seal">¥</span><span class="v2-proof-stars">ZENNU</span></div>
          <div class="v2-proof-label">高額ローン不要</div>
          <div class="v2-proof-value is-text">月額制</div>
          <div class="v2-proof-desc">月額10,890円〜・単月契約OK</div>
        </div>
        <div class="v2-proof">
          <div class="v2-proof-top"><span class="v2-proof-seal">G</span><span class="v2-proof-stars">★★★★★</span></div>
          <div class="v2-proof-label">Google口コミ評価</div>
          <div class="v2-proof-value is-review">4.9</div>
          <div class="v2-proof-desc">Googleビジネスプロフィール掲載時点</div>
        </div>
      </div>
    </div>
  </div>
  <div class="v2-hero-photo">
    <img src="/{{ site.hero.image }}" alt="ZenNuでパーソナルトレーニングに取り組む様子">
  </div>
  <a href="#reserve" class="v2-slot" aria-label="今月の無料体験を予約する">
    <small>今月の体験予約</small>
    <strong>残り{{ site.campaign.remaining }}</strong>
    <span>名様</span>
  </a>
</section>

<!-- ─── QUICK BENEFITS ─── -->
<div class="band v2-band">
  <p><span>過度な糖質制限なし</span><span>セルフ利用は回数無制限</span><span>月額制・単月契約OK</span></p>
</div>

<!-- ─── PAIN POINTS ─── -->
<section class="sec-cream" id="about">
  <div class="sec-content">
    <div class="v2-section-head rv">
      <div class="v2-eyebrow">WHY DIETS DON’T LAST</div>
      <h2 class="v2-title">痩せたいのに続かない。<br>それは、<em>意志の弱さ</em>だけが原因ではありません。</h2>
      <p class="v2-lead">自己流で頑張るほど、何を信じればいいか分からなくなり、生活に無理が出てしまう。まずは「続かなかった理由」を変えることから始めます。</p>
    </div>
    <div class="v2-pain-grid stagger">
      <div class="v2-pain-card"><span class="v2-pain-no">01</span><p class="v2-pain-text">食事を減らせば痩せると思い、我慢ばかりになる</p></div>
      <div class="v2-pain-card"><span class="v2-pain-no">02</span><p class="v2-pain-text">ジムに入っても、何をすればいいか分からない</p></div>
      <div class="v2-pain-card"><span class="v2-pain-no">03</span><p class="v2-pain-text">最初に頑張りすぎて、忙しくなると途切れてしまう</p></div>
      <div class="v2-pain-card"><span class="v2-pain-no">04</span><p class="v2-pain-text">一度体重が落ちても、元の生活と体型に戻ってしまう</p></div>
      <div class="v2-pain-card"><span class="v2-pain-no">05</span><p class="v2-pain-text">お腹・二の腕・下半身をどう変えるか迷っている</p></div>
      <div class="v2-pain-card"><span class="v2-pain-no">06</span><p class="v2-pain-text">人の多いジムが苦手で、だんだん足が遠のく</p></div>
    </div>
    <div class="v2-pain-answer rv">
      <b>THE ANSWER</b>
      <p>消費を増やす。食事を整える。変化を見て調整する。<br>この3つが、痩せ続ける土台です。</p>
    </div>
  </div>
</section>

<!-- ─── COMPACT CTA 01 ─── -->
<aside class="v2-mini-cta">
  <div class="v2-mini-cta-inner">
    <div class="v2-mini-cta-copy"><strong>まずは「何を変えれば痩せやすい？」を整理しませんか。</strong><span>手ぶらOK ／ 初回体験無料 ／ 無理な勧誘なし</span></div>
    <a href="#reserve" class="v2-mini-cta-link">空き状況を見る →</a>
  </div>
</aside>

<!-- ─── METHOD / CONCEPT ─── -->
<section class="sec-white" id="concept">
  <div class="sec-content">
    <div class="v2-section-head rv">
      <div class="v2-eyebrow">ZENNU METHOD</div>
      <h2 class="v2-title">なぜ、食べながらでも<br><em>痩せられる</em>のか。</h2>
      <p class="v2-lead">体脂肪が落ちる基本は、摂取エネルギーと消費エネルギーのバランス。ZenNuは食事だけを削るのではなく、筋トレで消費をつくり、必要な栄養を摂り、体の変化に合わせて調整します。</p>
    </div>
    <div class="v2-method-grid stagger">
      <article class="v2-method-card">
        <img src="/{{ site.concept.cards[0].image }}" alt="筋トレで全身を動かすパーソナルトレーニング" loading="lazy" decoding="async">
        <div class="v2-method-content"><span class="v2-method-no">01 / BURN</span><h3>筋肉を動かし、<br>消費をつくる。</h3><p>体の状態と目標に合わせた筋トレで、日常に運動による消費をプラス。正しいフォームで狙った部位を動かします。</p><span class="v2-method-effect">消費を増やす</span></div>
      </article>
      <article class="v2-method-card">
        <img src="/{{ site.concept.cards[2].image }}" alt="食事内容を無理なく整えるサポート" loading="lazy" decoding="async">
        <div class="v2-method-content"><span class="v2-method-no">02 / EAT</span><h3>3食を整え、<br>余計な我慢を減らす。</h3><p>炭水化物を含む過度な食事制限は推奨しません。必要な栄養を摂りながら、量と食べ方を見直します。</p><span class="v2-method-effect">摂取を整える</span></div>
      </article>
      <article class="v2-method-card">
        <img src="/{{ site.concept.cards[1].image }}" alt="体の変化に合わせてトレーニングを調整する様子" loading="lazy" decoding="async">
        <div class="v2-method-content"><span class="v2-method-no">03 / ADJUST</span><h3>数値と体調を見て、<br>やり方を調整する。</h3><p>体組成データとトレーニング状況を確認。変化が止まる前に、食事・フォーム・運動量を見直します。</p><span class="v2-method-effect">変化に合わせる</span></div>
      </article>
    </div>
  </div>
</section>

<!-- ─── ROUTINE BEFORE / AFTER ─── -->
<section class="v2-routine">
  <div class="sec-content">
    <div class="v2-section-head rv">
      <div class="v2-eyebrow">WHY IT WORKS</div>
      <h2 class="v2-title">結果が出るのは、<br><em>毎回の行動</em>が変わるから。</h2>
      <p class="v2-lead">ただ通うだけではなく、「何を食べるか」「どう動くか」「いつ調整するか」が明確になる。痩せるための行動を、プロと一緒に回す設計です。</p>
    </div>
    <div class="v2-routine-grid rv">
      <div class="v2-routine-card">
        <div class="v2-routine-label">BEFORE / 痩せにくい自己流</div>
        <ol class="v2-routine-list">
          <li><span>01</span>食事量だけを大きく減らす</li>
          <li><span>02</span>自己流で同じ運動を繰り返す</li>
          <li><span>03</span>数字が止まっても原因が分からない</li>
          <li><span>04</span>我慢が続かず、元の生活に戻る</li>
        </ol>
      </div>
      <div class="v2-routine-arrow" aria-hidden="true">→</div>
      <div class="v2-routine-card after">
        <div class="v2-routine-label">AFTER / ZenNuの痩せるサイクル</div>
        <ol class="v2-routine-list">
          <li><span>01</span>必要な栄養は摂り、食事内容を整える</li>
          <li><span>02</span>筋トレで全身を動かし、消費を増やす</li>
          <li><span>03</span>フォームと運動量をプロに確認する</li>
          <li><span>04</span>体の変化に合わせて食事と運動を調整する</li>
        </ol>
      </div>
    </div>
  </div>
</section>

<!-- ─── EARLY PROOF / BEFORE AFTER ─── -->
<section class="sec-white" id="voice">
  <div class="sec-content">
    <div class="v2-proof-intro rv">
      <div>
        <div class="v2-eyebrow">REAL MEMBER STORIES</div>
        <h2 class="v2-title">この仕組みで、<br><em>カラダはこう変わった。</em></h2>
      </div>
      <p class="v2-proof-note">掲載数値は各会員の実例です。結果には個人差があり、同様の変化を保証するものではありません。</p>
    </div>
    <div class="carousel" id="ba-carousel">
      <button class="car-arrow prev" id="ba-prev" aria-label="前へ">‹</button>
      <div class="car-viewport">
        <div class="car-track" id="ba-track">
          {% for v in site.memberVoice.items %}
          <div class="car-slide">
            <div class="ba-slide-inner">
              <div class="ba-photo"><img src="/{{ v.image }}" alt="{{ v.meta }} ビフォーアフター" loading="lazy" decoding="async"></div>
              <div class="ba-info">
                <div class="ba-tag">{{ v.tag }}</div>
                {% if v.story %}
                <div class="ba-meta-row">{{ v.meta }} ／ {{ v.story }}</div>
                {% else %}
                <div class="ba-meta-row">{{ v.meta }}</div>
                {% endif %}

                <div class="ba-stats">
                  {% if v.duration and v.weightKg %}
                  <div class="ba-stat-main">
                    <span class="ba-stat-pre">{{ v.duration }}</span>
                    <span class="ba-stat-num">{{ v.weightKg }}<span>kg</span></span>
                  </div>
                  {% endif %}
                  <div class="ba-stat-sub">
                    <div><span class="ba-stat-sub-label">ウエスト</span><span class="ba-stat-sub-num">{{ v.result }}<i>{{ v.unit }}</i></span></div>
                    {% if v.bodyFatPct %}
                    <div><span class="ba-stat-sub-label">体脂肪率</span><span class="ba-stat-sub-num">{{ v.bodyFatPct }}<i>%</i></span></div>
                    {% endif %}
                  </div>
                </div>

                {% if v.journey or v.currentRoutine %}
                <div class="ba-routine">
                  {% if v.journey %}
                  <div class="ba-journey">
                    <div class="ba-block-title">取り組みの変化</div>
                    <ol class="ba-journey-list">
                      {% for step in v.journey %}
                      <li><span class="ba-journey-num">{{ "0" + loop.index if loop.index < 10 else loop.index }}</span>{{ step }}</li>
                      {% endfor %}
                    </ol>
                  </div>
                  {% endif %}
                  {% if v.currentRoutine %}
                  <div class="ba-current">
                    <div class="ba-block-title">現在の習慣</div>
                    <div class="ba-current-box">
                      {% for line in v.currentRoutine %}
                      <div>{{ line }}</div>
                      {% endfor %}
                    </div>
                  </div>
                  {% endif %}
                </div>
                {% endif %}

                <p class="ba-quote">{{ v.quote }}</p>
                <div class="ba-who">{{ v.who }}</div>
              </div>
            </div>
          </div>
          {% endfor %}
        </div>
      </div>
      <button class="car-arrow next" id="ba-next" aria-label="次へ">›</button>
    </div>
    <div class="car-dots" id="ba-dots"></div>
    <p class="v2-section-footnote">※ ご本人の通い方と計測結果を、既存の会員データに基づいて掲載しています。</p>
  </div>
</section>

<!-- ─── COMPACT CTA 02 ─── -->
<aside class="v2-mini-cta">
  <div class="v2-mini-cta-inner">
    <div class="v2-mini-cta-copy"><strong>あなたが痩せるために変えるポイントを、体験で整理できます。</strong><span>今月のパーソナル体験予約は残り{{ site.campaign.remaining }}名様</span></div>
    <a href="#reserve" class="v2-mini-cta-link">無料体験を予約する →</a>
  </div>
</aside>

<!-- ─── NO HIGH-COST LOAN ─── -->
<section class="sec-cream" id="features">
  <div class="sec-content">
    <div class="v2-value">
      <div class="v2-value-copy rv">
        <div class="v2-eyebrow">NO HIGH-COST LOAN</div>
        <h2 class="v2-title">何十万円ものローンを<br><em>組まなくても始められる。</em></h2>
        <p class="v2-lead">最初に高額コースを契約するのではなく、目標と生活に合うプランを月額制で選ぶ。単月契約にも対応しているから、ダイエットを始めるために大きな決断をする必要はありません。</p>
        <div class="v2-value-price">
          <small>STARTING PRICE / 月額</small>
          <strong>¥10,890<sup>〜</sup></strong>
          <span>高額ローン不要 ／ 単月契約OK</span>
        </div>
      </div>
      <div class="v2-value-visual rv">
        <img src="/images/personal1.jpg" alt="トレーナーからダイエットのための筋力トレーニング指導を受ける様子" loading="lazy" decoding="async">
        <div class="v2-value-facts" aria-label="料金契約の特徴">
          <div class="v2-value-fact"><strong>月額制</strong>必要なプランを選べる</div>
          <div class="v2-value-fact"><strong>単月契約OK</strong>長期契約だけではありません</div>
          <div class="v2-value-fact"><strong>ローン不要</strong>分割払い前提の表示ではありません</div>
        </div>
      </div>
    </div>
    <div class="v2-value-availability rv" aria-label="セルフトレーニングの利用環境">
      <span>セルフ利用 7:00〜22:00</span>
      <span>同時利用4枠の少人数予約制</span>
      <span>現在40名／定員80名</span>
    </div>
    <p class="v2-section-footnote rv">パーソナルトレーニングは9:00〜21:00の90分枠でご案内しているため、体験枠には限りがあります。</p>
  </div>
</section>

<!-- ─── PRICE ─── -->
<section class="sec-white" id="price">
  <div class="sec-big-label rv">PRICE</div>
  <div class="sec-big-sub rv" style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:12px;">
    <span>{{ site.pricing.subtitle }}</span>
    <a href="#reserve" style="font-family:var(--display);font-weight:700;font-size:13px;letter-spacing:.08em;color:var(--orange);border-bottom:1px solid var(--orange);padding-bottom:2px;">
      まずは無料体験から →
    </a>
  </div>
  <div class="sec-content rv">
    <div class="price-grid stagger">
      {% for p in site.pricing.plans %}
      <div class="plan-card{% if p.popular %} popular{% endif %}">
        {% if p.popular %}<span class="popular-tag">MOST POPULAR</span>{% endif %}
        <div class="plan-top">
          <div class="plan-name">{{ p.name }}</div>
          <div class="plan-for">{{ p.for }}</div>
        </div>
        <div class="plan-body">
          <div class="plan-price">{{ p.price }}<span class="unit">{{ p.unit }}</span></div>
          <div class="plan-items">
            {% for it in p.items %}
            {% if "3名上限" in it %}
            <div class="plan-item">同時利用4枠の予約制で快適な環境</div>
            {% else %}
            <div class="plan-item">{{ it }}</div>
            {% endif %}
            {% endfor %}
          </div>
          <a href="#reserve" class="plan-cta">このプランを相談する</a>
        </div>
      </div>
      {% endfor %}
    </div>
    <div class="ticket-block rv">
      <div class="ticket-head">
        <div class="ticket-title">{{ site.pricing.ticket.title }}</div>
        <div class="ticket-sub">{{ site.pricing.ticket.sub }}</div>
      </div>
      <div class="ticket-grid stagger">
        {% for c in site.pricing.ticket.cards %}
        <div class="ticket-card{% if c.best %} best{% endif %} {{ 'tilt-l' if loop.first else 'tilt-r' }}">
          {% if c.badge %}<span class="ticket-badge">{{ c.badge }}</span>{% endif %}
          <div class="ticket-count">{{ c.count }}<span>回</span></div>
          <div class="ticket-price">{{ c.price }}</div>
          <div class="ticket-per">{{ c.per }}</div>
        </div>
        {% endfor %}
      </div>
    </div>

    <div class="addon-grid stagger">
      {% for a in site.pricing.addons %}
      <div class="addon-box {{ 'tilt-l' if loop.first else 'tilt-r' }}">
        <div class="addon-head{% if a.dark %} dark{% endif %}">{{ a.title }} <small>{{ a.en }}</small></div>
        <p class="addon-desc">{{ a.desc }}</p>
        <div class="addon-lines">
          {% for l in a.lines %}
          <div class="addon-line"><span>{{ l.label }}</span><strong>{{ l.price }}<small>{{ l.per }}</small></strong></div>
          {% endfor %}
        </div>
        <p class="addon-note">{{ a.note | safe }}</p>
      </div>
      {% endfor %}
    </div>

    <p class="price-note rv">{{ site.pricing.note | safe }}</p>
  </div>
</section>

<!-- ─── FEATURES ─── -->
<section class="sec-cream">
  <div class="sec-big-label rv">WHY ZENNU</div>
  <div class="sec-big-sub rv">ダイエットを一人で終わらせない4つの理由</div>
  <div class="sec-content rv">
    <div class="features-grid stagger">
      {% for f in site.features.items %}
      <div class="feat-card" data-num="{{ f.num }}">
        <h3 class="feat-title">{{ f.title }}</h3>
        <p class="feat-body">{{ f.body }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- ─── NUTRITION EXPERTISE ─── -->
<section class="sec-white" id="nutrition">
  <div class="sec-content">
    <div class="v2-nutrition-grid rv">
      <div class="v2-nutrition-copy">
        <div class="v2-eyebrow">NUTRITION EXPERTISE</div>
        <h2 class="v2-title">我慢だけに頼らない。<br><em>資格の知見</em>で食事を整える。</h2>
        <p class="v2-lead">ZenNuでは、臨床栄養医学指導士の知見を食事サポートに活用。食べる量をただ減らすのではなく、必要な栄養を摂りながら、ダイエットを続けられる食べ方を一緒に組み立てます。</p>
        <div class="v2-nutrition-points">
          <div class="v2-nutrition-point">
            <span class="v2-nutrition-check">✓</span>
            <div><strong>炭水化物を含む3食を基本に調整</strong><p>極端な糖質制限やカロリーカットに頼らず、量と食べ方を見直します。</p></div>
          </div>
          <div class="v2-nutrition-point">
            <span class="v2-nutrition-check">✓</span>
            <div><strong>体の変化に合わせてアドバイス</strong><p>体組成やトレーニング状況を確認し、変化が止まる前に内容を調整します。</p></div>
          </div>
          <div class="v2-nutrition-point">
            <span class="v2-nutrition-check">✓</span>
            <div><strong>必要な方だけ選べる食事管理</strong><p>食事管理は月額8,800円〜のオプション。目的に合わせて追加できます。</p></div>
          </div>
        </div>
      </div>
      <div class="v2-nutrition-visual">
        <img src="/images/trainer-mori-credential.png" alt="ZenNuの食事と運動をサポートするトレーナー" loading="lazy" decoding="async">
        <div class="v2-credential-card" aria-label="食事指導に活用している資格">
          <span class="v2-credential-seal">CERT.</span>
          <div><small>QUALIFICATION</small><strong>臨床栄養医学指導士</strong><span>食べながら痩せるための食事サポートに活用</span></div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── TRAINERS ─── -->
<section class="sec-white" id="trainers">
  <div class="sec-big-label rv">TRAINERS</div>
  <div class="sec-big-sub rv">{{ site.trainers.subtitle }}</div>
  <div style="margin-top:48px;">
    {% for t in site.trainers.items %}
    <div class="trainer-row{% if t.reverse %} reverse{% endif %} rv">
      <div class="trainer-panel">
        <div class="trainer-portrait"><img src="/{{ t.portrait }}" alt="{{ t.jp | striptags }}" loading="lazy" decoding="async"></div>
        <div class="trainer-info">
          <div class="trainer-jp">{{ t.jp }}</div>
          <div class="trainer-en">{{ t.en | safe }}</div>
          <p class="trainer-bio">{{ t.bio }}</p>
          <div class="trainer-tags">{% for tag in t.tags %}<span class="trainer-tag">{{ tag }}</span>{% endfor %}</div>
        </div>
      </div>
      <div class="trainer-action">
        {% if t.actionImage %}<img src="/{{ t.actionImage }}" alt="{{ t.jp | striptags }} トレーニング" loading="lazy" decoding="async">{% endif %}
      </div>
    </div>
    {% endfor %}
  </div>
</section>

<!-- ─── COMPARISON ─── -->
<section class="sec-cream" id="comparison">
  <div class="sec-content rv">
    <div class="v2-eyebrow">WHY ZENNU</div>
    <h2 class="v2-title">短期コースで終わらせない、<br>続けるための選択肢。</h2>
    <div class="comp-table-wrap">
      <table class="comp-table">
        <thead><tr><th></th><th class="th-other">{{ site.comparison.headOther }}</th><th class="th-zen">{{ site.comparison.headZen }}</th></tr></thead>
        <tbody>{% for r in site.comparison.rows %}<tr><td>{{ r.label }}</td><td>{{ r.other | safe }}</td><td class="td-zen">{{ r.zen | safe }}</td></tr>{% endfor %}</tbody>
      </table>
    </div>
  </div>
</section>

<!-- ─── FLOW ─── -->
<section class="sec-white">
  <div class="sec-big-label rv">FLOW</div>
  <div class="sec-big-sub rv">無料体験から、続く習慣へ</div>
  <div class="sec-content rv">
    <div class="flow-grid stagger">
      {% for step in site.flow.steps %}
      <div class="flow-step{% if step.accent %} accent{% endif %}"><div class="flow-num">{{ step.num }}</div><div class="flow-title">{{ step.title }}</div><div class="flow-desc">{{ step.desc }}</div></div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- ─── GALLERY ─── -->
<section class="sec-cream" id="gallery">
  <div class="sec-big-label rv">PLACE</div>
  <div class="sec-big-sub rv">人目と混雑を避けて集中できる空間</div>
  <div class="sec-content rv">
    <div class="gallery-grid stagger">
      {% for g in site.gallery.items %}
      <div class="g-item{% if g.size %} {{ g.size }}{% endif %}"><img src="/{{ g.image }}" alt="{{ g.label }}" loading="lazy" decoding="async"><span>{{ g.label }}</span></div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- ─── FAQ ─── -->
<section class="sec-white" id="faq">
  <div class="sec-big-label rv">FAQ</div>
  <div class="sec-big-sub rv">ダイエットと体験について、よくあるご質問</div>
  <div class="sec-content rv">
    <div class="faq-grid">
      {% for item in site.faq.items %}
      <div class="faq-item">
        <button class="faq-q">
          <span class="faq-q-mark">Q</span>
          <span class="faq-q-text">{{ item.q }}</span>
          <span class="faq-icon">+</span>
        </button>
        <div class="faq-a"><div class="faq-a-inner">
          <p>{{ item.a }}</p>
        </div></div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- ─── STORE INFO ─── -->
<section class="sec-cream" id="access">
  <div class="sec-big-label rv">INFO</div>
  <div class="sec-big-sub rv">店舗情報・アクセス</div>
  <div class="sec-content rv">
    <div class="access-grid">
      <div class="access-map">
        <iframe title="ZenNu WELLNESS DESIGN 地図" src="{{ site.access.mapEmbed }}" allowfullscreen loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
      </div>
      <div class="info-list">
        {% for row in site.access.rows %}
        <div class="info-row">
          <div class="info-label">{{ row.label }}</div>
          <div class="info-value">{{ row.value | safe }}</div>
        </div>
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<!-- ─── CTA (campaign) ─── -->
<section class="camp" id="cta">
  <div class="camp-bg" style="background-image:url('/images/hero.jpg');"></div>
  <div class="camp-inner rv">
    <div class="camp-ribbon">{{ site.cta.campaignTitle }}</div>
    <p class="camp-sub">続けられるダイエットの第一歩を、無料体験から。</p>

    <div class="camp-benefits">
      {% for b in site.cta.benefits %}
      <div class="camp-card">
        <span class="camp-no">特典<strong>{{ b.no }}</strong></span>
        <div class="camp-card-lead">{{ b.lead }}</div>
        <div class="camp-deal">
          <span class="camp-before">{{ b.before }}</span>
          <span class="camp-arrow">▶</span>
          <span class="camp-after">{{ b.after }}</span>
        </div>
      </div>
      {% endfor %}

      <div class="camp-card camp-gift">
        <span class="camp-no">特典<strong>{{ site.cta.gift.no }}</strong></span>
        <div class="camp-gift-body">
          {% if site.cta.gift.image %}
          <div class="camp-gift-img"><img src="/{{ site.cta.gift.image }}" alt="体組成計レポート見本" loading="lazy" decoding="async"></div>
          {% endif %}
          <div class="camp-gift-text">
            <div class="camp-gift-lead">{{ site.cta.gift.lead }}</div>
            <h3 class="camp-gift-title">{{ site.cta.gift.title }}</h3>
            <ul class="camp-gift-list">
              {% for g in site.cta.gift.items %}
              <li>{{ g | safe }}</li>
              {% endfor %}
            </ul>
          </div>
        </div>
      </div>
    </div>

    <div class="camp-foot">
      <div class="camp-limit">先着順！今月の体験予約枠 残り{{ site.campaign.remaining }}名様</div>
      <a href="#reserve" class="camp-btn">
        <span class="camp-btn-tag">手ぶらOK</span>
        <span class="camp-btn-main">無料体験を予約する</span>
        <span class="camp-btn-arr">→</span>
      </a>
      <p class="camp-note">{{ site.cta.note }}</p>
      <div class="camp-scroll">{{ site.cta.scroll }} ↓</div>
    </div>
  </div>
</section>

<!-- ─── RESERVATION (hacomono) ─── -->
<section class="sec-white" id="reserve">
  <div class="sec-content rv" style="padding-top: clamp(36px, 5vw, 60px);">
    <div class="reserve-widget">
      <iframe id="hacomono-widget-w0002" src="https://zennuwellnessdesign.hacomono.jp/widgets/2?isFilterableByCategory=false&isShowStudioInfo=true&studioIds=1" title="ZenNu 予約ウィジェット"></iframe>
    </div>
  </div>
</section>
<script src="https://zennuwellnessdesign.hacomono.jp/js/widget.js"></script>

<!-- ─── FOOTER ─── -->
{% include "footer.njk" %}

<!-- sticky -->
<div class="sticky-bar" id="sticky-bar">
  <a href="#reserve">無料体験を予約する（今月残り{{ site.campaign.remaining }}名） →</a>
</div>

<script>
  // header scroll
  const header = document.getElementById('site-header');
  window.addEventListener('scroll', () => {
    header.classList.toggle('scrolled', window.scrollY > 60);
  }, { passive: true });

  // hamburger
  const hamBtn = document.getElementById('ham-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  hamBtn.addEventListener('click', () => {
    const open = hamBtn.classList.toggle('active');
    mobileMenu.classList.toggle('open', open);
    document.body.style.overflow = open ? 'hidden' : '';
  });
  document.querySelectorAll('.nav-link').forEach(l => {
    l.addEventListener('click', () => {
      hamBtn.classList.remove('active');
      mobileMenu.classList.remove('open');
      document.body.style.overflow = '';
    });
  });

  // FAQ
  document.querySelectorAll('.faq-q').forEach(btn => {
    btn.addEventListener('click', () => {
      const item = btn.closest('.faq-item');
      const isOpen = item.classList.contains('open');
      document.querySelectorAll('.faq-item.open').forEach(el => el.classList.remove('open'));
      if (!isOpen) item.classList.add('open');
    });
  });

  // sticky bar
  const stickyBar = document.getElementById('sticky-bar');
  const ctaSec = document.getElementById('cta');
  const footer = document.querySelector('.site-footer');
  window.addEventListener('scroll', () => {
    const ct = ctaSec.getBoundingClientRect().top;
    const ft = footer.getBoundingClientRect().top;
    stickyBar.classList.toggle('hide', ct < window.innerHeight || ft < window.innerHeight + 60);
  }, { passive: true });

  // scroll reveal + stagger
  const obs = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) { e.target.classList.add('on'); obs.unobserve(e.target); }
    });
  }, { threshold: 0.06, rootMargin: '0px 0px -20px 0px' });
  document.querySelectorAll('.rv, .stagger').forEach(el => obs.observe(el));

  // stats count-up
  const countObs = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (!e.isIntersecting) return;
      countObs.unobserve(e.target);
      const to = parseInt(e.target.dataset.count, 10);
      const dur = 1300, start = performance.now();
      const tick = now => {
        const p = Math.min((now - start) / dur, 1);
        const ease = 1 - Math.pow(1 - p, 3);
        e.target.textContent = Math.round(to * ease);
        if (p < 1) requestAnimationFrame(tick);
      };
      requestAnimationFrame(tick);
    });
  }, { threshold: 0.5 });
  document.querySelectorAll('[data-count]').forEach(el => countObs.observe(el));

  // before/after carousel
  (() => {
    const track = document.getElementById('ba-track');
    if (!track) return;
    const slides = track.querySelectorAll('.car-slide');
    const prev = document.getElementById('ba-prev');
    const next = document.getElementById('ba-next');
    const dotsWrap = document.getElementById('ba-dots');
    let index = 0;

    slides.forEach((_, i) => {
      const dot = document.createElement('button');
      dot.className = 'car-dot' + (i === 0 ? ' active' : '');
      dot.setAttribute('aria-label', (i + 1) + '番目を表示');
      dot.addEventListener('click', () => goTo(i));
      dotsWrap.appendChild(dot);
    });
    const dots = dotsWrap.querySelectorAll('.car-dot');

    function goTo(i) {
      index = (i + slides.length) % slides.length;
      track.style.transform = `translateX(-${index * 100}%)`;
      dots.forEach((d, di) => d.classList.toggle('active', di === index));
    }
    prev.addEventListener('click', () => goTo(index - 1));
    next.addEventListener('click', () => goTo(index + 1));

    // swipe support
    let startX = null;
    const vp = track.parentElement;
    vp.addEventListener('touchstart', e => { startX = e.touches[0].clientX; }, { passive: true });
    vp.addEventListener('touchend', e => {
      if (startX === null) return;
      const dx = e.changedTouches[0].clientX - startX;
      if (Math.abs(dx) > 40) goTo(index + (dx < 0 ? 1 : -1));
      startX = null;
    }, { passive: true });
  })();
</script>
</body>
</html>
