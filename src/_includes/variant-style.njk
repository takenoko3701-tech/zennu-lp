<style>
  /* ─── 訴求バリエーション専用スタイル(LAUREL BADGE / PAIN POINTS / AUTHORITY) ─── */

  /* 月桂樹バッジ（ヒーローの実績数値） */
  .laurel-row {
    display: grid; grid-template-columns: repeat(3, clamp(88px, 10vw, 118px));
    gap: 10px;
  }
  .laurel-badge { position: relative; width: 100%; aspect-ratio: 1 / 1; }
  .laurel-svg { position: absolute; inset: 0; width: 100%; height: 100%; }
  .laurel-content {
    position: absolute; inset: 0;
    display: flex; flex-direction: column; align-items: center;
    padding-top: 35%;
  }
  .laurel-lbl {
    font-size: clamp(8px, .95vw, 10px); color: var(--muted); letter-spacing: .02em;
    margin-bottom: 3px; text-align: center; line-height: 1.3; max-width: 78%;
  }
  .laurel-num {
    font-family: var(--display); font-weight: 900; color: var(--text); line-height: 1;
    font-size: clamp(19px, 2.6vw, 27px);
  }
  .laurel-num span:last-child { font-size: 56%; color: var(--orange); margin-left: 1px; }
  .laurel-ribbon {
    position: absolute; left: 50%; bottom: 4%; transform: translateX(-50%);
    width: 76%; height: 15%;
    background: var(--orange);
    clip-path: polygon(6% 8%, 94% 8%, 100% 50%, 94% 92%, 6% 92%, 0% 50%);
    display: flex; align-items: center; justify-content: center;
    box-shadow: 0 2px 6px rgba(42,37,32,.18);
  }
  .laurel-ribbon span {
    font-family: var(--display); font-weight: 800; font-size: clamp(7px, .85vw, 9px);
    color: #fff; letter-spacing: .14em;
  }

  /* 悩み訴求 */
  .pain-grid {
    display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px 32px;
    max-width: 760px; margin: 0 auto 32px;
  }
  .pain-item {
    display: flex; align-items: center; gap: 12px;
    font-size: 15px; font-weight: 700; color: var(--text); letter-spacing: .02em;
    padding: 14px 18px; background: var(--white); border-radius: 10px;
    border: 1px solid var(--border);
  }
  .pain-mark {
    flex-shrink: 0; width: 26px; height: 26px; border-radius: 50%;
    background: var(--orange); color: #fff; font-weight: 900; font-size: 13px;
    display: flex; align-items: center; justify-content: center;
  }
  .pain-bridge {
    text-align: center; font-size: 15px; font-weight: 700; color: var(--orange);
    letter-spacing: .03em; max-width: 560px; margin: 0 auto;
  }

  /* 権威付け(写真＋資格証明) */
  .authority-grid {
    display: grid; grid-template-columns: 1fr 1fr; gap: clamp(32px, 5vw, 64px);
    align-items: center;
  }
  .authority-eyebrow {
    font-family: var(--display); font-weight: 700; font-size: 12px;
    color: var(--orange); letter-spacing: .2em; margin-bottom: 16px;
  }
  .authority-title {
    font-family: var(--serif); font-weight: 700;
    font-size: clamp(20px, 2.4vw, 27px); line-height: 1.6; letter-spacing: .02em;
    color: var(--text); margin-bottom: 22px;
  }
  .authority-list { display: flex; flex-direction: column; gap: 18px; }
  .authority-check { display: flex; gap: 10px; }
  .authority-check-mark {
    flex-shrink: 0; width: 22px; height: 22px; border-radius: 50%;
    background: var(--orange); color: #fff; font-size: 12px; font-weight: 900;
    display: flex; align-items: center; justify-content: center; margin-top: 2px;
  }
  .authority-check-body { font-size: 13.5px; color: var(--muted); line-height: 1.8; letter-spacing: .02em; }
  .authority-check-body strong { display: block; color: var(--text); font-size: 14.5px; font-weight: 700; margin-bottom: 2px; }

  .authority-visual { position: relative; }
  .authority-photo {
    aspect-ratio: 4 / 5; border-radius: 16px; overflow: hidden;
    background: var(--cream);
  }
  .authority-photo img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .authority-cert {
    position: absolute; right: -16px; bottom: -20px; width: 46%;
    background: var(--white); border: 1px solid var(--border); border-radius: 10px;
    box-shadow: 0 10px 30px rgba(42,37,32,.14);
    padding: 10px; display: flex; flex-direction: column; align-items: center; gap: 6px;
  }
  .authority-cert-placeholder {
    width: 100%; aspect-ratio: 3 / 4; border-radius: 6px;
    border: 1.5px dashed var(--border); background: var(--orange-pale);
    display: flex; align-items: center; justify-content: center;
    font-size: 10.5px; color: var(--muted); text-align: center; padding: 8px;
    line-height: 1.5;
  }

  /* ヒーロー：会員実例キャプション */
  .hero-headline-note {
    font-size: 12.5px; color: var(--light); letter-spacing: .02em;
    margin-top: 10px; margin-bottom: 0;
  }

  /* なぜ体重だけでは判断できないのか */
  .whyweight-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 28px;
    max-width: 980px; margin: 0 auto 48px;
  }
  .whyweight-card { text-align: center; padding: 0 8px; }
  .whyweight-num {
    font-family: var(--display); font-weight: 900; font-size: 13px;
    color: var(--orange); letter-spacing: .1em; margin-bottom: 10px;
  }
  .whyweight-title {
    font-family: var(--serif); font-weight: 700; font-size: 17px;
    color: var(--text); letter-spacing: .02em; margin-bottom: 10px; line-height: 1.5;
  }
  .whyweight-text { font-size: 13.5px; color: var(--muted); line-height: 1.85; letter-spacing: .02em; }

  .whyweight-proof {
    max-width: 720px; margin: 0 auto; padding-top: 36px; border-top: 1px solid var(--border);
    text-align: center;
  }
  .whyweight-proof-label {
    font-size: 12px; color: var(--muted); letter-spacing: .1em; margin-bottom: 20px;
  }
  .whyweight-proof-row {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-bottom: 24px;
  }
  .whyweight-proof-item {
    background: var(--cream); border-radius: 12px; padding: 18px 10px;
  }
  .whyweight-proof-num {
    font-family: var(--display); font-weight: 900; font-size: clamp(22px, 3vw, 28px);
    color: var(--orange); line-height: 1;
  }
  .whyweight-proof-num span { font-size: 60%; margin-left: 1px; }
  .whyweight-proof-lbl {
    font-size: 11.5px; color: var(--muted); margin-top: 8px; line-height: 1.5;
  }
  .whyweight-proof-link {
    display: inline-flex; align-items: center; gap: 6px;
    font-size: 13.5px; font-weight: 700; color: var(--orange); letter-spacing: .02em;
  }

  @media (max-width: 860px) {
    .pain-grid { grid-template-columns: 1fr; }
    .authority-grid { grid-template-columns: 1fr; }
    .authority-visual { max-width: 320px; margin: 0 auto; }
    .whyweight-grid { grid-template-columns: 1fr; gap: 32px; }
    .whyweight-proof-row { grid-template-columns: 1fr; }
  }
</style>
